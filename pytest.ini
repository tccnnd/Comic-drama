[pytest]
# T0.2: 仅固化机器无关配置；basetemp 由 scripts/test.ps1 与 CI 显式传递，
# 不在此写入 $env:TEMP 等机器相关路径（Windows 不会自动展开）。
testpaths = tests
norecursedirs =
    .git
    .venv
    .pytest_cache
    _external
    data/tmp_pytest*
    outputs
    tmp
    tools
    workspace
addopts = -p no:cacheprovider
