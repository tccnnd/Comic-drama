[pytest]
testpaths = tests
norecursedirs =
    .git
    .venv
    .pytest_cache
    _external
    data/tmp_pytest*
    outputs
    tmp
    tools
    workspace
addopts = -p no:cacheprovider --basetemp=data/tmp_pytest
