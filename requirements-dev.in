# 开发 / 测试 / 构建依赖（pip-compile 锁定为 requirements-dev.txt）
# 包含运行时全部依赖 + 开发工具，可用于完整开发环境同步。
-r requirements.in
pytest==9.0.3
pytest-cov==6.0.0
black
isort
mypy
bandit
pre-commit
# 依赖漏洞扫描：pip-audit（免费无登录墙；safety 2.x 需账号，见 CHANGELOG T2.5）
pip-audit
pip-tools
